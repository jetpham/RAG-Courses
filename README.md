# Lab 06 - CS272

## Start the database:
    ```sh
    rm -rf chromadb && docker compose up
    ```

## Running with prompt for querying
    ```sh
    go run .
    ```

## Running the test
    ```sh
    go test
    ```